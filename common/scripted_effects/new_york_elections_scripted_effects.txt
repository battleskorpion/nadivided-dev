SNY_recalc_election_icon_frames = {
    202 = {
        if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_democratic_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 1 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_republican_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 2 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_labor_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 3 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_socialist_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_labor_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 4 }
        }
        else = {
            set_variable = { current_frame = 5 }
        }
    }
    277 = {
        if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_democratic_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 1 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_republican_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 2 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_labor_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 3 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_socialist_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_labor_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 4 }
        }
        else = {
            set_variable = { current_frame = 5 }
        }
    }
    280 = {
        if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_democratic_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 1 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_republican_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 2 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_labor_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 3 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_socialist_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_labor_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 4 }
        }
        else = {
            set_variable = { current_frame = 5 }
        }
    }
    274 = {
        if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_democratic_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 1 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_republican_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 2 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_labor_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 3 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_socialist_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_labor_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 4 }
        }
        else = {
            set_variable = { current_frame = 5 }
        }
    }
    29 = {
        if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_democratic_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 1 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_republican_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 2 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_labor_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 3 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_socialist_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_labor_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 4 }
        }
        else = {
            set_variable = { current_frame = 5 }
        }
    }
    276 = {
        if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_democratic_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_democratic_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 1 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_republican_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_labor_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_republican_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 2 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_labor_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_socialist_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_labor_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 3 }
        }
        else_if = {
            limit = {
                AND = {
                    check_variable = { 
                        var = state_socialist_support
                        value = state_democratic_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_republican_support 
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_labor_support
                        compare = greater_than_or_equals
                    }
                    check_variable = { 
                        var = state_socialist_support
                        value = state_communist_support 
                        compare = greater_than_or_equals
                    }
                }
            }
            set_variable = { current_frame = 4 }
        }
        else = {
            set_variable = { current_frame = 5 }
        }
    }
}
SNY_set_and_randomize_popularities = {
    202 = {
        set_variable = { state_democratic_support = 59 }
		set_variable = { state_republican_support = 31 }
		set_variable = { state_labor_support = 5 }
		set_variable = { state_socialist_support = 3 }
		set_variable = { state_communist_support = 2 }

        set_temp_variable = { dem_temp = random }
        randomize_temp_variable = {
            var = dem_temp
            distribution = binomial
            min = 0
            max = 50
        }
        round_temp_variable = dem_temp
        add_to_variable = { state_democratic_support = dem_temp }
        subtract_from_variable = { state_republican_support = dem_temp }

        set_temp_variable = { rep_temp = random }
        randomize_temp_variable = {
            var = rep_temp
            distribution = binomial
            min = -20
            max = 30
        }
        round_temp_variable = rep_temp
        add_to_variable = { state_republican_support = rep_temp }
        subtract_from_variable = { state_democratic_support = rep_temp }

        set_temp_variable = { lab_temp = random }
        randomize_temp_variable = {
            var = lab_temp
            distribution = binomial
            min = -4
            max = 12
        }
        round_temp_variable = lab_temp
        add_to_variable = { state_labor_support = lab_temp }
        subtract_from_variable = { state_democratic_support = lab_temp }

        set_temp_variable = { soc_temp = random }
        randomize_temp_variable = {
            var = soc_temp
            distribution = binomial
            min = -2
            max = 6
        }
        round_temp_variable = soc_temp
        add_to_variable = { state_socialist_support = soc_temp }
        subtract_from_variable = { state_democratic_support = soc_temp }

        set_temp_variable = { com_temp = random }
        randomize_temp_variable = {
            var = com_temp
            distribution = binomial
            min = -1
            max = 3
        }
        round_temp_variable = com_temp
        add_to_variable = { state_communist_support = com_temp }
        subtract_from_variable = { state_democratic_support = com_temp }
    }
    277 = {
        set_variable = { state_democratic_support = 49 }
		set_variable = { state_republican_support = 44 }
		set_variable = { state_labor_support = 4 }
		set_variable = { state_socialist_support = 2 }
		set_variable = { state_communist_support = 1 }

        set_temp_variable = { dem_temp = random }
        randomize_temp_variable = {
            var = dem_temp
            distribution = binomial
            min = 0
            max = 45
        }
        round_temp_variable = dem_temp
        add_to_variable = { state_democratic_support = dem_temp }
        subtract_from_variable = { state_republican_support = dem_temp }

        set_temp_variable = { rep_temp = random }
        randomize_temp_variable = {
            var = rep_temp
            distribution = binomial
            min = -20
            max = 30
        }
        round_temp_variable = rep_temp
        add_to_variable = { state_republican_support = rep_temp }
        subtract_from_variable = { state_democratic_support = rep_temp }

        set_temp_variable = { lab_temp = random }
        randomize_temp_variable = {
            var = lab_temp
            distribution = binomial
            min = -4
            max = 10
        }
        round_temp_variable = lab_temp
        add_to_variable = { state_labor_support = lab_temp }
        subtract_from_variable = { state_democratic_support = lab_temp }

        set_temp_variable = { soc_temp = random }
        randomize_temp_variable = {
            var = soc_temp
            distribution = binomial
            min = -2
            max = 5
        }
        round_temp_variable = soc_temp
        add_to_variable = { state_socialist_support = soc_temp }
        subtract_from_variable = { state_democratic_support = soc_temp }

        set_temp_variable = { com_temp = random }
        randomize_temp_variable = {
            var = com_temp
            distribution = binomial
            min = -1
            max = 3
        }
        round_temp_variable = com_temp
        add_to_variable = { state_communist_support = com_temp }
        subtract_from_variable = { state_democratic_support = com_temp }
    }
    280 = {
        set_variable = { state_democratic_support = 49 }
		set_variable = { state_republican_support = 44 }
		set_variable = { state_labor_support = 4 }
		set_variable = { state_socialist_support = 2 }
		set_variable = { state_communist_support = 1 }

        set_temp_variable = { dem_temp = random }
        randomize_temp_variable = {
            var = dem_temp
            distribution = binomial
            min = 0
            max = 40
        }
        round_temp_variable = dem_temp
        add_to_variable = { state_democratic_support = dem_temp }
        subtract_from_variable = { state_republican_support = dem_temp }

        set_temp_variable = { rep_temp = random }
        randomize_temp_variable = {
            var = rep_temp
            distribution = binomial
            min = -10
            max = 30
        }
        round_temp_variable = rep_temp
        add_to_variable = { state_republican_support = rep_temp }
        subtract_from_variable = { state_democratic_support = rep_temp }

        set_temp_variable = { lab_temp = random }
        randomize_temp_variable = {
            var = lab_temp
            distribution = binomial
            min = -4
            max = 8
        }
        round_temp_variable = lab_temp
        add_to_variable = { state_labor_support = lab_temp }
        subtract_from_variable = { state_democratic_support = lab_temp }

        set_temp_variable = { soc_temp = random }
        randomize_temp_variable = {
            var = soc_temp
            distribution = binomial
            min = -2
            max = 4
        }
        round_temp_variable = soc_temp
        add_to_variable = { state_socialist_support = soc_temp }
        subtract_from_variable = { state_democratic_support = soc_temp }

        set_temp_variable = { com_temp = random }
        randomize_temp_variable = {
            var = com_temp
            distribution = binomial
            min = -1
            max = 2
        }
        round_temp_variable = com_temp
        add_to_variable = { state_communist_support = com_temp }
        subtract_from_variable = { state_democratic_support = com_temp }
    }
    274 = {
        set_variable = { state_democratic_support = 49 }
		set_variable = { state_republican_support = 44 }
		set_variable = { state_labor_support = 4 }
		set_variable = { state_socialist_support = 2 }
		set_variable = { state_communist_support = 1 }

        set_temp_variable = { dem_temp = random }
        randomize_temp_variable = {
            var = dem_temp
            distribution = binomial
            min = 0
            max = 35
        }
        round_temp_variable = dem_temp
        add_to_variable = { state_democratic_support = dem_temp }
        subtract_from_variable = { state_republican_support = dem_temp }

        set_temp_variable = { rep_temp = random }
        randomize_temp_variable = {
            var = rep_temp
            distribution = binomial
            min = -10
            max = 30
        }
        round_temp_variable = rep_temp
        add_to_variable = { state_republican_support = rep_temp }
        subtract_from_variable = { state_democratic_support = rep_temp }

        set_temp_variable = { lab_temp = random }
        randomize_temp_variable = {
            var = lab_temp
            distribution = binomial
            min = -4
            max = 8
        }
        round_temp_variable = lab_temp
        add_to_variable = { state_labor_support = lab_temp }
        subtract_from_variable = { state_democratic_support = lab_temp }

        set_temp_variable = { soc_temp = random }
        randomize_temp_variable = {
            var = soc_temp
            distribution = binomial
            min = -2
            max = 4
        }
        round_temp_variable = soc_temp
        add_to_variable = { state_socialist_support = soc_temp }
        subtract_from_variable = { state_democratic_support = soc_temp }

        set_temp_variable = { com_temp = random }
        randomize_temp_variable = {
            var = com_temp
            distribution = binomial
            min = -1
            max = 2
        }
        round_temp_variable = com_temp
        add_to_variable = { state_communist_support = com_temp }
        subtract_from_variable = { state_democratic_support = com_temp }
    }
    29 = {
        set_variable = { state_democratic_support = 49 }
		set_variable = { state_republican_support = 44 }
		set_variable = { state_labor_support = 4 }
		set_variable = { state_socialist_support = 2 }
		set_variable = { state_communist_support = 1 }

        set_temp_variable = { dem_temp = random }
        randomize_temp_variable = {
            var = dem_temp
            distribution = binomial
            min = 0
            max = 40
        }
        round_temp_variable = dem_temp
        add_to_variable = { state_democratic_support = dem_temp }
        subtract_from_variable = { state_republican_support = dem_temp }

        set_temp_variable = { rep_temp = random }
        randomize_temp_variable = {
            var = rep_temp
            distribution = binomial
            min = -20
            max = 30
        }
        round_temp_variable = rep_temp
        add_to_variable = { state_republican_support = rep_temp }
        subtract_from_variable = { state_democratic_support = rep_temp }

        set_temp_variable = { lab_temp = random }
        randomize_temp_variable = {
            var = lab_temp
            distribution = binomial
            min = -4
            max = 10
        }
        round_temp_variable = lab_temp
        add_to_variable = { state_labor_support = lab_temp }
        subtract_from_variable = { state_democratic_support = lab_temp }

        set_temp_variable = { soc_temp = random }
        randomize_temp_variable = {
            var = soc_temp
            distribution = binomial
            min = -2
            max = 5
        }
        round_temp_variable = soc_temp
        add_to_variable = { state_socialist_support = soc_temp }
        subtract_from_variable = { state_democratic_support = soc_temp }

        set_temp_variable = { com_temp = random }
        randomize_temp_variable = {
            var = com_temp
            distribution = binomial
            min = -1
            max = 3
        }
        round_temp_variable = com_temp
        add_to_variable = { state_communist_support = com_temp }
        subtract_from_variable = { state_democratic_support = com_temp }
    }
    276 = {
        set_variable = { state_democratic_support = 49 }
		set_variable = { state_republican_support = 44 }
		set_variable = { state_labor_support = 4 }
		set_variable = { state_socialist_support = 2 }
		set_variable = { state_communist_support = 1 }

        set_temp_variable = { dem_temp = random }
        randomize_temp_variable = {
            var = dem_temp
            distribution = binomial
            min = 0
            max = 30
        }
        round_temp_variable = dem_temp
        add_to_variable = { state_democratic_support = dem_temp }
        subtract_from_variable = { state_republican_support = dem_temp }

        set_temp_variable = { rep_temp = random }
        randomize_temp_variable = {
            var = rep_temp
            distribution = binomial
            min = 0
            max = 30
        }
        round_temp_variable = rep_temp
        add_to_variable = { state_republican_support = rep_temp }
        subtract_from_variable = { state_democratic_support = rep_temp }

        set_temp_variable = { lab_temp = random }
        randomize_temp_variable = {
            var = lab_temp
            distribution = binomial
            min = -4
            max = 4
        }
        round_temp_variable = lab_temp
        add_to_variable = { state_labor_support = lab_temp }
        subtract_from_variable = { state_democratic_support = lab_temp }

        set_temp_variable = { soc_temp = random }
        randomize_temp_variable = {
            var = soc_temp
            distribution = binomial
            min = -2
            max = 2
        }
        round_temp_variable = soc_temp
        add_to_variable = { state_socialist_support = soc_temp }
        subtract_from_variable = { state_democratic_support = soc_temp }

        set_temp_variable = { com_temp = random }
        randomize_temp_variable = {
            var = com_temp
            distribution = binomial
            min = -1
            max = 1
        }
        round_temp_variable = com_temp
        add_to_variable = { state_communist_support = com_temp }
        subtract_from_variable = { state_democratic_support = com_temp }
    }
    SNY_recalc_election_icon_frames = yes
}
SNY_campaign_decision = {
    if = {
        limit = {
            OR = {
                has_completed_focus = SNY_herbet_h_lehman
                has_completed_focus = SNY_endorse_herbet_h_lehman
            }
        }
        if = {
            limit = {
                check_variable = { state_republican_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_dem_support_tt
            add_to_variable = { state_democratic_support = 10 }
            subtract_from_variable = { state_republican_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_labor_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_dem_support_tt
            add_to_variable = { state_democratic_support = 10 }
            subtract_from_variable = { state_labor_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_socialist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_dem_support_tt
            add_to_variable = { state_democratic_support = 10 }
            subtract_from_variable = { state_socialist_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_communist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_dem_support_tt
            add_to_variable = { state_democratic_support = 10 }
            subtract_from_variable = { state_communist_support = 10 }
        }
        else = {
            custom_effect_tooltip = SNY_cannot_campaign_tt
        }
    }
    else_if = {
        limit = {
            OR = {
                has_completed_focus = SNY_william_f_bleakey
            }
        }
        if = {
            limit = {
                check_variable = { state_democratic_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_rep_support_tt
            add_to_variable = { state_republican_support = 10 }
            subtract_from_variable = { state_democratic_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_labor_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_rep_support_tt
            add_to_variable = { state_republican_support = 10 }
            subtract_from_variable = { state_labor_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_socialist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_rep_support_tt
            add_to_variable = { state_republican_support = 10 }
            subtract_from_variable = { state_socialist_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_communist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_rep_support_tt
            add_to_variable = { state_republican_support = 10 }
            subtract_from_variable = { state_communist_support = 10 }
        }
        else = {
            custom_effect_tooltip = SNY_cannot_campaign_tt
        }
    }
    else_if = {
        limit = {
            OR = {
                has_completed_focus = SNY_rockwell_kent
            }
        }
        if = {
            limit = {
                check_variable = { state_democratic_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_lab_support_tt
            add_to_variable = { state_labor_support = 10 }
            subtract_from_variable = { state_democratic_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_republican_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_lab_support_tt
            add_to_variable = { state_labor_support = 10 }
            subtract_from_variable = { state_republican_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_socialist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_lab_support_tt
            add_to_variable = { state_labor_support = 10 }
            subtract_from_variable = { state_socialist_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_communist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_lab_support_tt
            add_to_variable = { state_labor_support = 10 }
            subtract_from_variable = { state_communist_support = 10 }
        }
        else = {
            custom_effect_tooltip = SNY_cannot_campaign_tt
        }
    }
    else_if = {
        limit = {
            OR = {
                has_completed_focus = SNY_harry_w_laidler
            }
        }
        if = {
            limit = {
                check_variable = { state_democratic_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_soc_support_tt
            add_to_variable = { state_socialist_support = 10 }
            subtract_from_variable = { state_democratic_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_republican_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_soc_support_tt
            add_to_variable = { state_socialist_support = 10 }
            subtract_from_variable = { state_republican_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_labor_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_soc_support_tt
            add_to_variable = { state_socialist_support = 10 }
            subtract_from_variable = { state_labor_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_communist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_soc_support_tt
            add_to_variable = { state_socialist_support = 10 }
            subtract_from_variable = { state_communist_support = 10 }
        }
        else = {
            custom_effect_tooltip = SNY_cannot_campaign_tt
        }
    }
    else_if = {
        limit = {
            OR = {
                has_completed_focus = SNY_robert_minor
            }
        }
        if = {
            limit = {
                check_variable = { state_democratic_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_cum_support_tt
            add_to_variable = { state_communist_support = 10 }
            subtract_from_variable = { state_democratic_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_republican_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_cum_support_tt
            add_to_variable = { state_communist_support = 10 }
            subtract_from_variable = { state_republican_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_labor_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_cum_support_tt
            add_to_variable = { state_communist_support = 10 }
            subtract_from_variable = { state_labor_support = 10 }
        }
        else_if = {
            limit = {
                check_variable = { state_socialist_support > 9 }
            }
            custom_effect_tooltip = SNY_gain_cum_support_tt
            add_to_variable = { state_communist_support = 10 }
            subtract_from_variable = { state_socialist_support = 10 }
        }
        else = {
            custom_effect_tooltip = SNY_cannot_campaign_tt
        }
    }
}