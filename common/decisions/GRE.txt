GRE_colonization = {
    GRE_243_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 243
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 243
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 243 }
        highlight_states = {
            state = 243
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            243 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_439_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 439
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 439
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 439 }
        highlight_states = {
            state = 439
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            439 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_455_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 455
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 455
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 455 }
        highlight_states = {
            state = 455
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            455 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_442_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 442
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 442
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 442 }
        highlight_states = {
            state = 442
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            442 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_643_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 643
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 643
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 643 }
        highlight_states = {
            state = 643
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            643 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_657_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 657
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 657
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 657 }
        highlight_states = {
            state = 657
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            657 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_437_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 437
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 437
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 437 }
        highlight_states = {
            state = 437
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            437 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_434_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 434
                }
            }
            NOT = {
                owns_state = 434
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 434 }
        highlight_states = {
            state = 434
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            434 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_435_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 435
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 435
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 435 }
        highlight_states = {
            state = 435
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            435 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_436_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 436
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 436
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 436 }
        highlight_states = {
            state = 436
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            436 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_655_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 655
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 655
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 655 }
        highlight_states = {
            state = 655
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            655 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_656_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 656
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 656
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 656 }
        highlight_states = {
            state = 656
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            656 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_645_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 645
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 645
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 645 }
        highlight_states = {
            state = 645
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            645 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_448_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 448
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 448
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 448 }
        highlight_states = {
            state = 448
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            448 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_644_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 644
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 644
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 644 }
        highlight_states = {
            state = 644
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            644 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_456_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 456
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 456
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 456 }
        highlight_states = {
            state = 456
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            456 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_494_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 494
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 494
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 494 }
        highlight_states = {
            state = 494
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            494 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_447_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 447
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 447
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 447 }
        highlight_states = {
            state = 447
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            447 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_453_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 453
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 453
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 453 }
        highlight_states = {
            state = 453
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            453 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_449_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 449
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 449
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 449 }
        highlight_states = {
            state = 449
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            449 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_490_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 490
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 490
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 490 }
        highlight_states = {
            state = 490
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            490 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_457_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 457
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 457
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 457 }
        highlight_states = {
            state = 457
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            457 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    
    GRE_450_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 450
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 450
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 450 }
        highlight_states = {
            state = 450
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            450 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_668_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 668
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 668
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 668 }
        highlight_states = {
            state = 668
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            668 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_669_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 669
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 669
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 669 }
        highlight_states = {
            state = 669
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            669 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_670_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 670
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 670
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 670 }
        highlight_states = {
            state = 670
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            670 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_648_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 648
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 648
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 648 }
        highlight_states = {
            state = 648
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            648 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_667_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 667
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 667
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 667 }
        highlight_states = {
            state = 667
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            667 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_405_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 405
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 405
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 405 }
        highlight_states = {
            state = 405
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            405 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_529_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 529
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 529
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 529 }
        highlight_states = {
            state = 529
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            529 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_493_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 493
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 493
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 493 }
        highlight_states = {
            state = 493
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            493 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_495_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 495
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 495
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 495 }
        highlight_states = {
            state = 495
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            495 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_244_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 244
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 244
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 244 }
        highlight_states = {
            state = 244
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            244 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_460_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 460
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 460
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 460 }
        highlight_states = {
            state = 460
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            460 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    
    GRE_680_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 680
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 680
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 680 }
        highlight_states = {
            state = 680
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            680 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_679_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 679
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 679
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 679 }
        highlight_states = {
            state = 679
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            679 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_530_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 530
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 530
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 530 }
        highlight_states = {
            state = 530
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            530 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_678_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 678
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 678
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 678 }
        highlight_states = {
            state = 678
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            678 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_491_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 491
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 491
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 491 }
        highlight_states = {
            state = 491
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            491 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_452_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 452
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 452
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 452 }
        highlight_states = {
            state = 452
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            452 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_659_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 659
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 659
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 659 }
        highlight_states = {
            state = 659
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            659 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_658_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 658
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 658
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 658 }
        highlight_states = {
            state = 658
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            658 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_647_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 647
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 647
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 647 }
        highlight_states = {
            state = 647
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            647 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_459_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 459
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 459
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 459 }
        highlight_states = {
            state = 459
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            459 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_492_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 492
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 492
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 492 }
        highlight_states = {
            state = 492
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            492 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_666_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 666
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 666
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 666 }
        highlight_states = {
            state = 666
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            666 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_665_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 665
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 665
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 665 }
        highlight_states = {
            state = 665
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            665 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_664_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 664
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 664
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 664 }
        highlight_states = {
            state = 664
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            664 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_663_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 663
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 663
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 663 }
        highlight_states = {
            state = 663
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            663 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_662_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 662
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 662
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 662 }
        highlight_states = {
            state = 662
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            662 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_661_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 661
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 661
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 661 }
        highlight_states = {
            state = 661
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            661 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_660_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 660
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 660
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 660 }
        highlight_states = {
            state = 660
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            660 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_672_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 672
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 672
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 672 }
        highlight_states = {
            state = 672
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            672 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_671_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 671
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 671
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 671 }
        highlight_states = {
            state = 671
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            671 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_675_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 675
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 675
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 675 }
        highlight_states = {
            state = 675
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            675 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_676_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 676
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 676
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 676 }
        highlight_states = {
            state = 676
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            676 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_674_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 674
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 674
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 674 }
        highlight_states = {
            state = 674
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            674 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_673_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 673
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 673
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 673 }
        highlight_states = {
            state = 673
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            673 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    
    GRE_646_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 646
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 646
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 646 }
        highlight_states = {
            state = 646
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            646 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_677_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 677
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 677
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 677 }
        highlight_states = {
            state = 677
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            677 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_438_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 438
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 438
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 438 }
        highlight_states = {
            state = 438
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            438 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_496_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 496
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 496
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 496 }
        highlight_states = {
            state = 496
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            496 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_458_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 458
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 458
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 458 }
        highlight_states = {
            state = 458
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            458 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
    GRE_508_colonization = {
        icon = GFX_

        available = {
            any_owned_state = {
                #limit = {
                #    NOT = {
                #        has_variable = colonization_progress
                #    }
                #}
                any_neighbor_state = {
                    state = 508
                }
            }
            NOT = {
                has_country_flag = GRE_already_colonizing
            }
            NOT = {
                owns_state = 508
            }
            has_manpower > 100
            has_equipment = {
                infantry_equipment > 9
            }
        }
        visible = {
            243 = {
                has_variable = colonization_progress
            }
        }
        cost = 50
        ai_will_do = {
            base = 100
            modifier = {
                factor = 2
                has_political_power > 175
            }
        }
        highlight_state_targets = { state = 508 }
        highlight_states = {
            state = 508
        }
        on_map_mode = map_only
        fire_only_once = no
        cancel_if_not_visible = yes

        complete_effect = {
            custom_effect_tooltip = GRE_colonization_tt
            custom_effect_tooltip = GRE_colonization_progress_243_tt
            hidden_effect = {
                set_country_flag = GRE_already_colonizing
            }
            508 = {
                set_variable = { colonization_progress = 10 }
            }
        }
    }
}