progressivism_on_the_rise = {
	prepare_for_progressive_civil_war = {

		icon = generic_prepare_civil_war

		available = {
			democratic > 0.1
		}

		visible = {
			NOT = { has_country_flag = preparation_for_progressive_civil_war }
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
		
		fire_only_once = yes
		
		cost = 50
		
		complete_effect = {
			set_country_flag = preparation_for_progressive_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	expand_civil_progressive_support = {

		icon = generic_civil_support

		visible = {
			OR = {
				has_country_flag = preparation_for_progressive_civil_war
				has_country_flag = open_up_political_discourse_progressive
			}
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}

		cancel_trigger = {
			hidden_trigger = {
				has_civil_war = yes
			}
		}
		
		fire_only_once = no
		
		cost = 25

		days_remove = 30

		modifier = {
			progressivism_drift = 0.02
			stability_weekly = -0.03
 			political_power_gain = -0.2
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	
	army_support_for_progressive_civil_war = {

		icon = generic_army_support

		visible = {
			has_country_flag = preparation_for_progressive_civil_war
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}

		cancel_trigger = {
			hidden_trigger = {
				has_civil_war = yes
			}
		}
	
		fire_only_once = no
	
		cost = 50

		days_remove = 30

		modifier = {
			command_power_gain = -0.2
		}

		remove_effect = {
			if = {
				limit = {
					check_variable = { var = army_support_var value = 0.1 compare = less_than }
				}
				set_variable = { var = army_support_var value = 0.1 } 
			}
			if = {
				limit = {
					check_variable = { var = army_support_var value = 0.1 compare = greater_than_or_equals }
				}
				add_to_variable = { var = army_support_var value = 0.1 } 
			}
			set_country_flag = military_support_for_progressive_civil_war
		}
		
		complete_effect = {
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	ensure_general_loyalty_for_progressive_civil_war = {

		icon = generic_army_support

		available = {
			has_country_flag = military_support_for_progressive_civil_war
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_progressive_civil_war
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			any_army_leader = {
				is_field_marshal = no
				NOT = { has_trait = hidden_sympathies }
			}
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			country_event = { id = decisions.1 }
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	ensure_marshal_loyalty_for_progressive_civil_war = {

		icon = generic_army_support

		available = {
			has_country_flag = military_support_for_progressive_civil_war
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_progressive_civil_war
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			any_army_leader = {
				NOT = { has_trait = hidden_sympathies }
			}
			#check_has_focus_tree_to_switch_to_democratic = yes
			any_army_leader = {
				is_field_marshal = yes
				NOT = { has_unit_leader_flag = hidden_sympathies_marshal }
			}
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			country_event = { id = decisions.2 }
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	siphon_equipment_stockpiles_for_progressive_civil_war = {

		icon = generic_prepare_civil_war

		available = {
			has_country_flag = military_support_for_progressive_civil_war
			has_equipment = { infantry_equipment > 1000 }
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_progressive_civil_war
			has_country_flag = military_support_for_progressive_civil_war
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
	
		fire_only_once = no
	
		cost = 50
		
		complete_effect = {
			add_command_power = -2
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -1000
			}
			set_country_flag = siphoned_equipment_stockpiles_for_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	formulate_surprise_attack_plan_for_progressive_civil_war = {

		icon = generic_army_support

		available = {
			has_country_flag = military_support_for_progressive_civil_war
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_progressive_civil_war
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			add_command_power = -2
			custom_effect_tooltip = formulate_surprise_attack_plan_for_civil_war_tt
			set_country_flag = formulated_surprise_attack_plan_for_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	ignite_the_progressive_civil_war = {

		icon = generic_ignite_civil_war

		available = {
			has_stability < 0.5
		}

		visible = {
			has_country_flag = preparation_for_progressive_civil_war
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			if = {
				limit = {
					has_government = fascism
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@fascism
				}
			}
			if = {
				limit = {
					has_government = communism
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@communism
				}
			}
			if = {
				limit = {
					has_government = neutrality
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@neutrality
				}
			}
			if = {
				limit = {
					has_government = democratic
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@democratic
				}
			}
			if = {
				limit = {
					has_government = longism
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@longism
				}
			}
			subtract_from_variable = {
				var = civil_war_size_var
				value = army_support_var
			}
			if = {
				limit = {
					check_variable = {
						var = civil_war_size_var
						value = 0.3
						compare = less_than
					}
				}
				set_variable = {
					var = civil_war_size_var
					value = 0.3
				}
			}
			start_civil_war = {
				ruling_party = progressivism
				ideology = ROOT
				size = civil_war_size_var
				keep_unit_leaders_trigger = {
					has_trait = hidden_sympathies
				}
			}
			clr_country_flag = preparation_for_progressive_civil_war
			clr_country_flag = military_support_for_progressive_civil_war
			clr_country_flag = civil_support_for_progressive_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	rebuild_the_nation_progressive = {

		icon = generic_construction

		visible = {
			has_government = progressivism
			has_country_flag = won_ideology_civil_war
		}

		fire_only_once = yes
	
		cost = 25

		days_remove = 365

		modifier = {
			political_power_gain = -1
			industry_repair_factor = 0.20
			production_speed_industrial_complex_factor = 0.1
			production_speed_infrastructure_factor = 0.1
			industrial_capacity_factory = 0.1
			industrial_capacity_dockyard = 0.1
		}
		
		ai_will_do = {
			factor = 200
		}
	}

	open_up_political_discourse_progressive = {

		icon = generic_political_discourse

		available = {
			fascism < 0.9
			democratic < 0.9
			communism < 0.9
			neutrality < 0.9
			progressivism < 0.9
			longism < 0.9
		}

		visible = {
			has_idea_with_trait = progressive_reformer
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
		
		remove_trigger = {
			has_government = progressivism
		}
		
		fire_only_once = yes
		
		cost = 75
		
		complete_effect = {
			set_country_flag = open_up_political_discourse_progressive
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	
	discredit_government_progressivism = {

		icon = generic_political_discourse

		available = {
			fascism < 0.8
			democratic < 0.8
			communism < 0.8
			neutrality < 0.8
			progressivism < 0.8
			longism < 0.8
			has_idea_with_trait = progressive_reformer
		}

		visible = {
			has_country_flag = open_up_political_discourse_progressive
			NOT = { has_government = progressivism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
		
		remove_trigger = {
			has_government = progressivism
		}
		
		fire_only_once = yes
		
		cost = 50
		
		complete_effect = {
			add_stability = -0.010
			if = {
				limit = {
					has_government = fascism
				}
				add_popularity = {
					ideology = fascism
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = communism
				}
				add_popularity = {
					ideology = communism
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = neutrality
				}
				add_popularity = {
					ideology = neutrality
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = democratic
				}
				add_popularity = {
					ideology = democratic
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = longism
				}
				add_popularity = {
					ideology = longism
					popularity = -0.1
				}
			}
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	
	hold_the_progressive_national_referendum = {

		icon = generic_political_discourse

		available = {
			progressivism > 0.5
		}

		visible = {
			NOT = { has_government = progressivism }
			OR = {
				progressivism > 0.6
				has_country_flag = open_up_political_discourse_progressive
			}
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
		
		remove_trigger = {
			has_government = progressivism
		}
		
		fire_only_once = yes
		
		cost = 100
		
		complete_effect = {
			country_event = { id = political.31 }
		}
		
		ai_will_do = {
			factor = 210
			modifier = {
				progressivism > 0.6
				factor = 10
			}
		}
	}
}
populism_on_the_rise = {
	prepare_for_populist_civil_war = {

		icon = generic_prepare_civil_war

		available = {
			democratic > 0.1
		}

		visible = {
			NOT = { has_country_flag = preparation_for_populist_civil_war }
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
		}
		
		fire_only_once = yes
		
		cost = 50
		
		complete_effect = {
			set_country_flag = preparation_for_populist_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	expand_civil_populist_support = {

		icon = generic_civil_support

		visible = {
			OR = {
				has_country_flag = preparation_for_populist_civil_war
				has_country_flag = open_up_political_discourse_populist
			}
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
		}

		cancel_trigger = {
			hidden_trigger = {
				has_civil_war = yes
			}
		}
		
		fire_only_once = no
		
		cost = 25

		days_remove = 30

		modifier = {
			longism_drift = 0.02
			stability_weekly = -0.03
 			political_power_gain = -0.2
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	
	army_support_for_populist_civil_war = {

		icon = generic_army_support

		visible = {
			has_country_flag = preparation_for_populist_civil_war
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
		}

		cancel_trigger = {
			hidden_trigger = {
				has_civil_war = yes
			}
		}
	
		fire_only_once = no
	
		cost = 50

		days_remove = 30

		modifier = {
			command_power_gain = -0.2
		}

		remove_effect = {
			if = {
				limit = {
					check_variable = { var = army_support_var value = 0.1 compare = less_than }
				}
				set_variable = { var = army_support_var value = 0.1 } 
			}
			if = {
				limit = {
					check_variable = { var = army_support_var value = 0.1 compare = greater_than_or_equals }
				}
				add_to_variable = { var = army_support_var value = 0.1 } 
			}
			set_country_flag = military_support_for_populist_civil_war
		}
		
		complete_effect = {
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	ensure_general_loyalty_for_populist_civil_war = {

		icon = generic_army_support

		available = {
			has_country_flag = military_support_for_populist_civil_war
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_populist_civil_war
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
			any_army_leader = {
				is_field_marshal = no
				NOT = { has_trait = hidden_sympathies }
			}
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			country_event = { id = decisions.1 }
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	ensure_marshal_loyalty_for_populist_civil_war = {

		icon = generic_army_support

		available = {
			has_country_flag = military_support_for_populist_civil_war
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_populist_civil_war
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
			any_army_leader = {
				NOT = { has_trait = hidden_sympathies }
			}
			#check_has_focus_tree_to_switch_to_democratic = yes
			any_army_leader = {
				is_field_marshal = yes
				NOT = { has_unit_leader_flag = hidden_sympathies_marshal }
			}
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			country_event = { id = decisions.2 }
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	siphon_equipment_stockpiles_for_populist_civil_war = {

		icon = generic_prepare_civil_war

		available = {
			has_country_flag = military_support_for_populist_civil_war
			has_equipment = { infantry_equipment > 1000 }
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_populist_civil_war
			has_country_flag = military_support_for_populist_civil_war
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
	
		fire_only_once = no
	
		cost = 50
		
		complete_effect = {
			add_command_power = -2
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -1000
			}
			set_country_flag = siphoned_equipment_stockpiles_for_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	formulate_surprise_attack_plan_for_populist_civil_war = {

		icon = generic_army_support

		available = {
			has_country_flag = military_support_for_populist_civil_war
			command_power > 2
		}

		visible = {
			has_country_flag = preparation_for_populist_civil_war
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			add_command_power = -2
			custom_effect_tooltip = formulate_surprise_attack_plan_for_civil_war_tt
			set_country_flag = formulated_surprise_attack_plan_for_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	ignite_the_populist_civil_war = {

		icon = generic_ignite_civil_war

		available = {
			has_stability < 0.5
		}

		visible = {
			has_country_flag = preparation_for_populist_civil_war
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
	
		fire_only_once = yes
	
		cost = 50
		
		complete_effect = {
			if = {
				limit = {
					has_government = fascism
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@fascism
				}
			}
			if = {
				limit = {
					has_government = communism
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@communism
				}
			}
			if = {
				limit = {
					has_government = neutrality
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@neutrality
				}
			}
			if = {
				limit = {
					has_government = democratic
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@democratic
				}
			}
			if = {
				limit = {
					has_government = longism
				}
				set_variable = {
					var = civil_war_size_var
					value = party_popularity@longism
				}
			}
			subtract_from_variable = {
				var = civil_war_size_var
				value = army_support_var
			}
			if = {
				limit = {
					check_variable = {
						var = civil_war_size_var
						value = 0.3
						compare = less_than
					}
				}
				set_variable = {
					var = civil_war_size_var
					value = 0.3
				}
			}
			start_civil_war = {
				ruling_party = longism
				ideology = ROOT
				size = civil_war_size_var
				keep_unit_leaders_trigger = {
					has_trait = hidden_sympathies
				}
			}
			clr_country_flag = preparation_for_populist_civil_war
			clr_country_flag = military_support_for_populist_civil_war
			clr_country_flag = civil_support_for_populist_civil_war
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	rebuild_the_nation_populist = {

		icon = generic_construction

		visible = {
			has_government = longism
			has_country_flag = won_ideology_civil_war
		}

		fire_only_once = yes
	
		cost = 25

		days_remove = 365

		modifier = {
			political_power_gain = -1
			industry_repair_factor = 0.20
			production_speed_industrial_complex_factor = 0.1
			production_speed_infrastructure_factor = 0.1
			industrial_capacity_factory = 0.1
			industrial_capacity_dockyard = 0.1
		}
		
		ai_will_do = {
			factor = 200
		}
	}

	open_up_political_discourse_populist = {

		icon = generic_political_discourse

		available = {
			fascism < 0.9
			democratic < 0.9
			communism < 0.9
			neutrality < 0.9
			progressivism < 0.9
			longism < 0.9
		}

		visible = {
			has_idea_with_trait = populist_reformer
			NOT = { has_government = longism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
		
		remove_trigger = {
			has_government = longism
		}
		
		fire_only_once = yes
		
		cost = 75
		
		complete_effect = {
			set_country_flag = open_up_political_discourse_populist
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	
	discredit_government_longism = {

		icon = generic_political_discourse

		available = {
			fascism < 0.8
			democratic < 0.8
			communism < 0.8
			neutrality < 0.8
			progressivism < 0.8
			longism < 0.8
			has_idea_with_trait = populist_reformer
		}

		visible = {
			has_country_flag = open_up_political_discourse_populist
			NOT = { has_government = longism }
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
		
		remove_trigger = {
			has_government = longism
		}
		
		fire_only_once = yes
		
		cost = 50
		
		complete_effect = {
			add_stability = -0.010
			if = {
				limit = {
					has_government = fascism
				}
				add_popularity = {
					ideology = fascism
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = communism
				}
				add_popularity = {
					ideology = communism
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = neutrality
				}
				add_popularity = {
					ideology = neutrality
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = democratic
				}
				add_popularity = {
					ideology = democratic
					popularity = -0.1
				}
			}
			if = {
				limit = {
					has_government = progressivism
				}
				add_popularity = {
					ideology = progressivism
					popularity = -0.1
				}
			}
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	
	hold_the_populist_national_referendum = {

		icon = generic_political_discourse

		available = {
			longism > 0.5
		}

		visible = {
			NOT = { has_government = longism }
			OR = {
				longism > 0.6
				has_country_flag = open_up_political_discourse_populist
			}
			#check_has_focus_tree_to_switch_to_democratic = yes
		}
		
		remove_trigger = {
			has_government = longism
		}
		
		fire_only_once = yes
		
		cost = 100
		
		complete_effect = {
			country_event = { id = political.30 }
		}
		
		ai_will_do = {
			factor = 210
			modifier = {
				longism > 0.6
				factor = 10
			}
		}
	}
}