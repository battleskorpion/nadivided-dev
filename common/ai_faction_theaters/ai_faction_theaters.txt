#USA

new_england_usa = {
	name = theater_northeast_usa
	regions = {
		1 #southern new negland
		38 #northern new england
	}
	cancel = {
		has_war = no
		#OR = {
		#	AND= {
		#		# Germany should stop focusing on the Western Front when Barbarossa starts
		#		original_tag = GER
		#		has_war_with = SOV
		#		219 = { is_controlled_by_ROOT_or_ally = no }
		#	}
		#	AND = {
		#		# UK should not care overly much about the continent while France is Capitulated
		#		FRA = {
		#			is_in_faction_with = ROOT
		#			has_capitulated = yes 
		#		}
		#	}
		#	has_war = no 
		#}
	}

	ai_will_do = {
		base = 0 
		modifier = {
			add = 1
			capital_scope = { is_on_continent = northeastern_usa }
		}
		modifier = {
			add = 5
			OR = {
				has_war_with = SMA
				has_war_with = SCT
				has_war_with = SRI
				has_war_with = SME
			}
		}
		#modifier = {
		#	factor = 10
		#	is_historical_focus_on = yes
		#	original_tag = USA
		#	is_in_faction_with = ENG
		#}
		modifier = {
			factor = 0
			has_war = no 
		}
		#modifier = {
		#	factor = 0
		#	original_tag = GER 
		#	has_war_with = SOV
		#	219 = { is_controlled_by_ROOT_or_ally = no } # We can focus again on the west if we take Moscow
		#}
		#modifier = {
		#	factor = 0
		#	original_tag = ENG 
		#	FRA = {
		#		is_in_faction_with = ROOT
		#		has_capitulated = yes 
		#	}
		#}
	} 
}

western_sea_board = {
	name = theater_western_seaboard_usa
	regions = {
		16 # 
		17 # 
		113 #
		11 # 
	}
	cancel = {
		has_war = no 
	}

	ai_will_do = {
		base = 0 
		modifier = {
			add = 1
			capital_scope = { is_on_continent = western_usa }
		}
		modifier = {
			add = 1
			OR = {
				has_war_with = SCA
				has_war_with = SOR
				has_war_with = SRO
				has_war_with = SWA
			}
		}
		#modifier = {
		#	add = 10
		#	original_tag = SOV
		#	has_war_with = GER
		#}
		modifier = {
			factor = 0
			has_war = no 
		}
	} 
}

southern_mountain_usa = { 
	name = theater_southern_mountain_usa
	regions = {
		19 #
		127 
		18 
		28 # 
		112 #

	}

	cancel = {
		has_war = no
	}

	ai_will_do = {
		base = 0 
		modifier = {
			add = 1
			OR = {
				has_war_with = SAZ
				has_war_with = SNM
				has_war_with = SCO
				has_war_with = NAV
			}
		}
	} 
}
