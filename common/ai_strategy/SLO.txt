SLO_does_the_eastern_front = { #Pfot, SLO will share front similar to Italian behavior
	allowed = { 
		tag = SLO
	}

	enable = {
		GER = {
			going_war_with_SOV = yes
			#is_ai = yes #You'd get in the way of a player
		}
		ROOT = {
			is_subject_of = GER
			surrender_progress < 0.01 #Nothing bad is happening at home
			any_neighbor_country = {
				NOT = {
					has_war_with = ROOT
				}
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = force_defend_ally_borders
		id = ROM
		value = 100
	}
	ai_strategy = {
		type = force_defend_ally_borders
		id = HUN
		value = 100
	}
	ai_strategy = {
		type = dont_defend_ally_borders
		id = GER
		value = 100
	}
	ai_strategy = {
    	type = front_unit_request
    	tag = SOV
    	value = 100
	}
}